"""
End-to-end integration tests for PicoMsg.

These tests generate code, compile it, and verify the output works correctly.
They require external dependencies (like Rust compiler) and are marked
appropriately so they can be skipped if dependencies are not available.
""" 
